[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "MASSIVE"
version = "0.1.0-dev"
description = "MALDI-ToF Analysis & Scoring Software for In Vitro Experiments"
requires-python = ">=3.10"